<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Dagger2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:textViewStyle">@style/TextViewCursive.Night</item>
        <item name="materialButtonStyle">@style/ButtonCursive.Night</item>
    </style>

    <style name="TextViewCursive.Night">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ButtonCursive.Night">
        <item name="android:backgroundTint">@color/white</item>
    </style>
</resources>